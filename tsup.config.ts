import { defineConfig } from "tsup";

export default defineConfig({
  entry: [
    "server.ts",
    "app.ts",
    "controllers/**/*.ts",
    "middleware/**/*.ts",
    "models/**/*.ts",
    "routes/**/*.ts",
    "utils/**/*.ts",
    "config/**/*.ts",
    "types/**/*.ts",
  ],
  outDir: "dist",
  target: "es2020",
  format: ["cjs"],
  splitting: false,
  sourcemap: true,
  clean: true,
  dts: true,
  treeshake: true,
  skipNodeModulesBundle: true,
  noExternal: [],
  external: [
    "bcryptjs",
    "body-parser",
    "cloudinary",
    "cookie-parser",
    "cors",
    "crypto-js",
    "dotenv",
    "express",
    "express-fileupload",
    "jsonwebtoken",
    "mongodb",
    "mongoose",
    "swagger-jsdoc",
    "swagger-ui-express",
    "validator",
  ],
});
